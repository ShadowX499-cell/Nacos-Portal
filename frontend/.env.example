# API base URL (Vite proxy rewrites /api → backend in dev, so leave as-is for dev)
VITE_API_URL=http://localhost:5000/api/v1

# App name shown in page titles / emails
VITE_APP_NAME=NACOS-AIFUE Portal
